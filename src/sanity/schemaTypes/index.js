import {listingType} from './listingType'
import {pageType} from './pageType'
import {locationType} from './locationType'
import {settingsType} from './settingsType'

export const schema = {
  types: [listingType, pageType, locationType, settingsType],
}
